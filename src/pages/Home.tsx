import Header from "@components/Header";
import MainTitle from "@components/MainTitle";

const Home = () => {
  return (
    <div>
    <Header/>
    <MainTitle />
  </div>
  )
};

export default Home;
